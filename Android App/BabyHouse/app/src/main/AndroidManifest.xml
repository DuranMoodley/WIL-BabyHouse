<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lalucia.babyhouse.babyhouse">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".StartScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlogPosts"
            android:label="@string/title_activity_blog_posts"
            android:parentActivityName=".NavigationDrawerStartScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Gallery"
            android:label="@string/title_activity_gallery"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OurTeam"
            android:label="@string/title_activity_our_team"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterScreen"
            android:label="@string/title_activity_register_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationScreen"
            android:configChanges="orientation"
            android:label="@string/title_activity_donation_screen"
            android:parentActivityName=".NavigationDrawerStartScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationsLandscape"
            android:label="@string/title_activity_donations_landscape"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationDetail"
            android:configChanges="orientation"
            android:label="@string/title_activity_donation_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdoptionProcess1"
            android:label="@string/title_activity_adoption"
            android:parentActivityName=".NavigationDrawerStartScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HelpScreen1"
            android:label="@string/title_activity_help"
            android:parentActivityName=".NavigationDrawerStartScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VolunteerProcess1"
            android:label="@string/title_activity_volunteer"
            android:parentActivityName=".NavigationDrawerStartScreen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeProcess1"
            android:label="@string/title_activity_help"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".NavigationDrawerStartScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".WishListProcess1"
            android:label="@string/title_activity_wish_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".BabyHouseMap"
            android:label="@string/title_activity_baby_house_map" />
        <activity
            android:name=".VolunteerCheckIn"
            android:label="@string/title_activity_volunteer_check_in"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>