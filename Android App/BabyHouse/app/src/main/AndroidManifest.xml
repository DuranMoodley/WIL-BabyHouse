<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lalucia.babyhouse.babyhouse">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.location.gps" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/babyhouseicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BlogPosts"
            android:label="@string/title_activity_blog_posts"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegisterScreen"
            android:label="@string/title_activity_register_screen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationScreen"
            android:configChanges="orientation"
            android:label="@string/title_activity_donation_screen"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationsLandscape"
            android:label="@string/title_activity_donations_landscape"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DonationDetail"
            android:configChanges="orientation"
            android:label="@string/title_activity_donation_detail"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AdoptionProcess"
            android:label="@string/title_activity_adoption"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HelpInformation"
            android:label="@string/title_activity_help"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VolunteerProcess"
            android:label="@string/title_activity_volunteer"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".MainActivity" />
        <activity
            android:name=".WishListInformation"
            android:label="@string/title_activity_wish_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA7e9KJObb3hFA1VsU80osaSSufgnVK4qU" />

        <activity
            android:name=".BabyHouseMap"
            android:label="@string/title_activity_baby_house_map" />
        <activity
            android:name=".VolunteerCheckIn"
            android:label="@string/title_activity_volunteer_check_in"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Events"
            android:label="@string/title_activity_events"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <service
            android:name=".GeofenceTransitionIntentService"
            android:enabled="true"
            android:exported="true" />
        <activity
            android:name=".ContactProcess"
            android:label="@string/title_activity_contact_process"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OurStaff"
            android:label="@string/title_activity_our_staff"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".Gallery"
            android:label="@string/title_activity_gallery"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".MainActivity">
        </activity>
    </application>

</manifest>